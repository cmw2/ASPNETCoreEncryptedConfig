{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "AllowedHosts": "*",
  "SecretKey": "secret", // put in real values during pipeline deployment, use secrets management during local dev
  "Secrets": {
    "SecretKey1": "secret1", // put in real values during pipeline deployment, use secrets management during local dev
    "SecretKey2": "secret2" // put in real values during pipeline deployment, use secrets management during local dev
  },
  "EncryptionOptions": {
    "Enabled": false,
    "EncryptedKeys": [] //{
    // let encryptor tool write this section
    //  "0": "SecretKey",
    //  "1": "Secrets:SecretKey1",
    //  "2": "Secrets:SecretKey2"
    //}
  }
}

