{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft": "Warning",
      "Microsoft.Hosting.Lifetime": "Information"
    }
  },
  "ConnectionStrings": {
    "DefaultConnection": "Server=tcp:cmwelastic.database.windows.net,1433;Initial Catalog=db1;Persist Security Info=False;User ID=chwestbr;Password={your_password};MultipleActiveResultSets=False;Encrypt=True;TrustServerCertificate=False;Connection Timeout=30;"
  },
  "AllowedHosts": "*",
  "SecretKey": "Some Secret !", // put in real values during pipeline deployment, use secrets management during local dev
  "Secrets": {
    "SecretKey1": "secret1", // put in real values during pipeline deployment, use secrets management during local dev
    "SecretKey2": "secret2" // put in real values during pipeline deployment, use secrets management during local dev
  },
  // Provide empty config section for encryptor tool to fill in
  "EncryptionOptions": {
    "Enabled": false,
    "EncryptedKeys": {}
  }
}

